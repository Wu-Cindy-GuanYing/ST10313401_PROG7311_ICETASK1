<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>@ViewData["Title"] - Online CV</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <header class="topbar">
        <div class="container topbar-inner">
            <a class="brand" href="/">CV</a>

            <nav class="nav">
                <a class="navlink" href="/">View CV</a>
                <a class="navlink" href="/cv.pdf">Download PDF</a>

                @if (User?.Identity?.IsAuthenticated == true)
                {
                    <a class="navlink" href="/admin">Admin</a>
                    <form method="post" action="/account/logout" class="inline">
                        @Html.AntiForgeryToken()
                        <button class="navlink btnlink" type="submit">Logout</button>
                    </form>
                }
                else
                {
                    <a class="navlink" href="/account/login">Admin Login</a>
                }
            </nav>
        </div>
    </header>

    <main class="container page">
        @RenderBody()
    </main>

    @RenderSection("Scripts", required: false)
</body>
</html>